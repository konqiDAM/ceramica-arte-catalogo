---
import BaseLayout from "../layouts/BaseLayout.astro";
import { url } from "../lib/url";

// Lista de imágenes de la galería (generada automáticamente por el script)
// Ejecuta: npm run update-galeria para actualizar esta lista cuando añadas imágenes
const fotos = [
  "/images/galeria/galeria-1.jpg",
];
---

<BaseLayout title="Galería de Fotos" description="Galería de fotos de piezas de cerámica y arte hechas a mano.">
  <section class="page-head">
    <div class="container">
      <p class="eyebrow">Jenifer Lazcano Sindin</p>
      <h1>Galería de Fotos</h1>
      <p class="lead">
        Impresiones del proceso artesanal.
      </p>
    </div>
  </section>

  <section class="section">
    <div class="container">
      <div class="gallery">
        {
          fotos.map((src, i) => (
            <figure class="gallery-item">
              <img src={url(src)} alt={`Fotografía ${i + 1} del estudio`} loading="lazy" width="600" height="600" />
            </figure>
          ))
        }
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .page-head { padding: clamp(2.5rem, 6vw, 4rem) 0 0; }
  .page-head .lead { margin-top: 0.5rem; }
  .gallery {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
    gap: 1.25rem;
  }
  .gallery-item {
    margin: 0;
    border-radius: var(--radius);
    overflow: hidden;
    box-shadow: var(--shadow);
  }
  .gallery-item img {
    display: block;
    width: 100%;
    height: auto;
    max-height: 300px;
    object-fit: contain;
    margin: 0 auto;
    transition: transform 0.3s ease;
  }
  .gallery-item:hover img { transform: scale(1.04); }
</style>
